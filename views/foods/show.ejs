<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Food Details - CookBook</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    
    <div class="container">
      <h1>Food Details</h1>
      
      <div class="card">
        <div class="card-body">
          <p><strong>Name:</strong> <%= food.name %></p>
          
          <div style="margin-top: 20px;" class="item-actions">
            <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit" class="button">Edit</a>
            <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST" class="inline">
              <button type="submit" class="button button-danger">Delete</button>
            </form>
          </div>
        </div>
      </div>
      
      <div style="margin-top: 20px;">
        <a href="/users/<%= user._id %>/foods" class="button button-secondary">Back to Pantry</a>
      </div>
    </div>
  </body>
</html>