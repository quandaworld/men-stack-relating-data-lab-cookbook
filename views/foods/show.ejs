<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Food Details</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Food Details</h1>
    <p><strong>Name:</strong> <%= food.name %></p>
    <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit" class="button">Edit</a>
    <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST" style="display:inline;">
      <button type="submit">Delete</button>
    </form>
    <br>
    <a href="/users/<%= user._id %>/foods" class="button">Back to Pantry</a>
  </body>
</html>