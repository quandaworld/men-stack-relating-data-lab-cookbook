<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Item - CookBook</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    
    <div class="container">
      <h1>Edit Item in Pantry</h1>
      
      <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=PUT" method="POST">
        <label for="name">Food Name:</label>
        <input type="text" id="name" name="name" value="<%= food.name %>" required>
        
        <button type="submit" class="button">Update Item</button>
        <a href="/users/<%= user._id %>/foods/<%= food._id %>" class="button button-secondary">Cancel</a>
      </form>
    </div>
  </body>
</html>